
CREATE DATABASE `student`;

USE `student`; CREATE TABLE students (id int PRIMARY KEY, Nom Varchar (250));

INSERT INTO 'students' ('id','nom') VALUES ('1','HADI').

SELECT  nom, note FROM eleve INNER JOIN note ON eleve.idf = note.idf;

